specVersion: "v1"
serviceId: "accidentreporting"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "accidentreporting-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
